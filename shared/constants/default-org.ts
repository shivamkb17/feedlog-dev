// Default organization constants. All data belongs to this organization;
// `resolveOrgContext()` returns these values, and migration seeds a
// matching `organization` row.

export const DEFAULT_ORG_ID = 'default-org'
export const DEFAULT_ORG_SLUG = 'default'
export const DEFAULT_ORG_NAME = 'Default'
